# 启动文本（按场景选用）

说明：每个场景一段启动指令，直接复制到新对话即可。手册全文见《发生发展逻辑模型_运行时配置操作手册_v2.5.md》。

---

## 场景A：日常对话（网页版/App，最常用）

目的：让模型按方法论回答你的日常问题。

**完整版（最可靠）**：
1. 打开新对话，粘贴《发生发展逻辑模型_运行时配置操作手册_v2.5.md》全文
2. 下一句发：请按上述手册规则工作，随后等我指令。

**精简版（不想贴全文）**：
> 请按"发生发展逻辑"工作：①先判交互类别（礼貌问候/情感需求/事实陈述与解释/功能实现）；②多问题先拆子问题、按依赖排序逐个回答；③事实回答只给判断（是/否/无法确认）＋来源＋边界，数值必带来源，无来源标"未核验"；④功能任务递归分解到问题清楚，先覆盖全部要求再压缩篇幅，不可行就明说并给替代和止损；⑤输出前按手册自检。随后等我指令。

---

## 场景B：接手仓库维护（协作者新对话）

目的：让新对话的模型获得项目全部上下文。
1. 粘贴《启动说明_v1.md》全文
2. 下一句发：请完整阅读并遵守以上启动说明，随后等我指令。
3. 需要按方法论工作时补一句：方法论规则见《发生发展逻辑模型_运行时配置操作手册_v2.5.md》，一并加载。

---

## 场景C：DeepSeek Harness 内使用（从零开始）

Harness 是什么：DeepSeek 官方的本地运行环境（本机已装 DSH 0.1.1-rc.2）——它负责装配模型、插件（skill）、工具，你在浏览器里和模型对话。skill 就是装进 Harness 的"方法论包"。

**第 1 步：启动 Harness**
1. 打开 PowerShell，运行：dsh web
2. 等它输出地址后，浏览器打开 http://127.0.0.1:3080
3. 在页面里新建对话，模型即可使用（模型账号在 Harness 设置里配置）

**第 2 步：安装方法论 skill（只需装一次，本地文件安装）**
方式一（推荐）：先 cd 到你的项目根目录，再运行：
cd "你的项目目录"
powershell -ExecutionPolicy Bypass -File "投递包路径\install_skill.ps1"
（不传参时 skill 装进当前目录的 .dsh\skills）
方式二（手动）：把 SKILL.md 复制到 项目根\.dsh\skills\occurrence-development-logic\SKILL.md（目录不存在就新建）
验证：新开会话后 Harness 能识别 /occurrence-development-logic；注意旧版 skill定义.json 的安装方式已废弃（当前 Harness 只识别 SKILL.md＋YAML frontmatter）

**第 3 步：在对话里使用（两种触发方式）**
- 方式一（手动）：在对话框输入 /occurrence-development-logic 后回车，再发你的任务；
- 方式二（口头）：直接说"请加载 occurrence-development-logic skill 并按它工作，然后帮我……"；
- 自动触发：skill 通过 frontmatter 的 description/whenToUse 路由，遇到长程任务、多问题复合请求等适用场景，模型会自己调用它（此时对话里会出现 skill 的调用记录）。

**第 4 步：确认生效**
模型回答如果出现这些特征，说明 skill 已生效：先判交互类别、多问题拆开逐个答、事实回答带"判断/来源/边界"、数值带来源、首次会话输出一张出入对照表（仅一次）。没生效就重发第 3 步的指令。

**常见问题**
- /名称 没反应 → 改用口头方式，或先确认项目根 .dsh\skills\occurrence-development-logic\SKILL.md 存在；
- 想暂时不用方法论 → 说"本次对话不要使用该 skill"；
- skill 更新了新版本 → 重新运行第 2 步的 add 命令覆盖，然后开新对话。

---

## 场景D：API / 脚本（开发者复现）

前提：$env:DEEPSEEK_API_KEY = "sk-你的key"（key 不能含中文）。
- deepseek-chat：System Prompt ＝ 手册全文；
- deepseek-reasoner / v4-pro：忽略 system 消息，把手册全文并入第一条 user 消息（"手册全文\n\n---\n\n用户问题"）；
- 完整实现参考 ab_test.py（含重试、温度控制、断点续跑、usage 落盘）。

---

## 场景E：评审方复测（官方团队 / 第三方评审）

前提：Python 3（仅标准库）＋ 已设置 DEEPSEEK_API_KEY。
步骤（在仓库根目录按顺序执行）：
1. python 题库工具_v3.py —— 从官方题库原始\ 重新生成题库_v8.json（种子固定，可跳过）
2. python ab_test.py —— 三臂生成（40 题×3臂×3模型≈360 次调用，断点续跑）
3. python grade.py —— 裁判盲评＋第二裁判＋Bonferroni 校正
4. python token统计.py —— 长度双口径统计
判据：《测试包_v8.md》第四节（预先声明）；数据口径：《数据摘要_v8.md》；投稿底稿：本仓库 Issue #1（1frank-art/occurrence-development-logic#1）。
