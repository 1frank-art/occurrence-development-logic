# 启动说明 v1（新对话启动文件）

> 用法：新对话的第一句粘贴本文件全文，模型即获得项目全部上下文；之后说"按第X项执行"即可。

## 一、项目一句话
发生发展逻辑模型（ODL）＝把"发生与发展"方法论引入模型输出的"上下文约束层"的校准型 System Prompt/skill；目的：提升 Agent 输出纪律与长程一致性（供公开加载、复测与评审）。

## 二、适用对象
维护者＝作者马子豪（天津大学）。本文件供维护者在新对话中加载项目上下文，第三方复测者按第七节操作。表达纪律：事实与判断分开、改动留痕、数据说话。

以"复测者/使用者"身份打开本仓库（第一次见到、想独立复现测试），按第七节「复测前置条件与预期」操作即可，无需承担维护职责。

## 三、文件地图（仓库：github.com/1frank-art/occurrence-development-logic）
| 文件 | 何时读 |
|---|---|
| 发生发展逻辑模型_运行时配置操作手册_v2.5.md | 修改手册、回答方法问题 |
| 测试包_v8.md ＋ 题库_v8.json | 修改测试（三臂：裸/手册/placebo） |
| 数据摘要_v8.md | 引用数据、对外汇报 |
| ab_test.py / grade.py / token统计.py | 跑测试、改脚本 |
| SKILL.md / install_skill.ps1 | 打包/安装 skill |
| 使用与投递说明_v1.md | 发布、投递与复现 |
| 启动文本_分场景.md | 各场景启动语（日常对话/项目接手/Harness/API/评审复测） |
| 分析报告_方法论如何提升AI能力.md | 机制解释与演示 |

## 四、核心事实（避免新对话重新研究）
- 手册四步：发生（四类交互：问候/情感/事实评价/功能实现，多问题先拆子问题）→ 发展（时空树＋位置；主层=同消息内多问题逻辑梳理，次层=跨对话锚点制）→ 应用（按类回应；事实评价=是/否/无法确认＋来源＋边界；功能实现=递归分解至目的达成，效率与token为优化指标）→ 自检（6条：五条＋角色判定一条）；
- 测试：86题（v7 题组46引用＋v8 新题组40：TruthfulQA 20/IFEval 20）、0生物题；对标 SimpleQA/TruthfulQA/FActScore/IFEval/MT-Bench；裁判盲评（deepseek-chat，同族偏见已披露）；判据测试前预先声明；
- v7 三臂实测（已完成）：来源引用率 +67.8pp、边界声明率 +36.7pp、要素覆盖严格率 +19.0pp，均显著（95%CI），placebo 无效应、零副作用 → 预声明判据1通过（v4 为早期历史版本：两臂、无 placebo、无统计）。v8 三臂实测（已完成）：G7 幻觉 +3.3pp 不显著（TruthfulQA 官方题为天花板，原生 91.7% 诚实）；G8 IFEval 严格率三臂均 60%（格式服从域，无效应）；长度效率 B 组 -69% 且质量持平（v2.5 修复出入对照表条款生效）；双裁判 kappa=0.466 → 判据1未成立，如实混合报告（数据摘要 v8）；
- 边界：单轮、三个公开模型（deepseek-chat/reasoner/v4-pro）；跨对话锚点制为预留条款、未测；
- 版本纪律：任何改动版本递增＋变更记录；原始输出与评分表由脚本本地生成、不随仓库分发；API key 不得写入任何文件。

## 五、当前状态与下一步
- 当前状态：v7+v8 测试完成（v7 判据1成立；v8 判据1未成立、如实混合报告）；投递已发：awesome-deepseek-integration PR #725（待 review）＋ harness Discussions #4604（主渠道：PR；备：Discussions）；
- 下一步（公开）：①维护仓库、响应 Issue 与社区反馈；②如需迭代（长度精炼等）改手册后重测并升版本；③欢迎第三方复现（见第七节）。

## 六、新对话启动语（直接复制）
"请完整阅读并遵守以下启动说明，随后等我指令：[粘贴本文件全文]"

## 七、复测前置条件与预期（面向第一次复现的陌生人）
- 环境：Python 3（仅标准库、无第三方依赖；实测 3.14.7）；
- Key：Windows PowerShell：$env:DEEPSEEK_API_KEY="sk-xxx"；Linux/macOS：export DEEPSEEK_API_KEY="sk-xxx"；
- 文件位置（仓库根目录）：手册＝发生发展逻辑模型_运行时配置操作手册_v2.5.md（B 组加载对象）；题库＝题库_v8.json；原始基准数据＝官方题库原始\TruthfulQA.csv、官方题库原始\ifeval_prompts.jsonl（题库工具_v3.py 抽样用）；脚本＝题库工具_v3.py、ab_test.py、grade.py、token统计.py；口径文档＝测试包_v8.md、数据摘要_v8.md。
- 流程（在仓库根目录按顺序执行）：①python 题库工具_v3.py——从官方题库原始\ 重新抽样生成题库_v8.json（种子固定，可跳过）；②python ab_test.py——三臂生成（40 新题×3臂×3模型≈360 次调用，约 1.5 小时，断点续跑）；③python grade.py——裁判盲评＋第二裁判（deepseek-reasoner）＋IFEval 程序判定＋Bonferroni 校正＋kappa；④python token统计.py——长度双口径（本仓库 results\ab_summary.csv；v7 历史 CSV 可作参数传入）。
- 产出：results\（原始输出）、评分表_v8.csv、token统计_v8.csv（本地生成，不随仓库分发）。
- 预期：B 组来源率/边界率高于 A（v7 引用指标）；B 组长度短于 A 且质量不降；G7 三臂诚实率约 91-95%（天花板）；G8 三臂约 60%（格式域无差异）——方向与 README 一致即可，数值可差几个百分点；
- 方向相反时：先查 Key 是否正确、手册是否完整加载（B 组 system prompt 或首条 user 消息）；仍异常，到仓库提 Issue（github.com/1frank-art/occurrence-development-logic/issues）。

## 八、常见任务与对应动作
- "改手册"→读手册v2.5→提出修改→留痕升版本；
- "跑测试"→确认 DEEPSEEK_API_KEY → python ab_test.py → python grade.py；
- "汇报"→读数据摘要_v8.md，按事实说话；
- "发布与提Issue"→读使用与投递说明_v1.md。