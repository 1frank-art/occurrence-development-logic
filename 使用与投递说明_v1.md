# 使用与投递说明 v1（怎么用＋关注与合作预期）

本仓库：https://github.com/1frank-art/occurrence-development-logic；主投递渠道：deepseek-ai/deepseek-harness Issue #1（https://github.com/deepseek-ai/deepseek-harness/issues/1）。

## 一、怎么用（4步）

第0步 本地验证：脚本已备（无第三方依赖）。PowerShell 里：$env:DEEPSEEK_API_KEY="sk-xxx"; python ab_test.py（三臂，最多约396次）→ python grade.py（公开协议评分，裁判盲评）→ 评分表_v7.csv。组6（跨对话锚点制）预留不测。
第1步 打包发布：README＋手册v2.4＋数据摘要v7＋测试包v7＋题库v7＋脚本×4＋skill定义＋启动说明/启动文本＋LICENSE，一个文件夹。
第2步 投递：主渠道＝deepseek-ai/deepseek-harness 仓库提 issue（仓库名以官方页面为准，模板见下）。
第3步 发布后维护：响应 Issue 与社区反馈；每次改动按变更记录递增版本。

## 二、Issue 模板（备渠道）
标题：[Skill 投稿] 发生发展逻辑模型（ODL）：面向 Agent 输出纪律与长程一致性的轻量约束层
正文：
这是一个可运行的 System Prompt/skill 约束模块：多问题复合请求的子问题拆分与逻辑梳理（主层）、跨对话锚点制（次层）、强制按类回答（问候/情感/事实评价/功能实现）、时空树定位、边界与来源声明、递归分解至目的达成，效率与 token 为优化指标。
附：手册v2.4（MIT）、46题题库v7（SimpleQA官方抽样＋自拟；三臂：裸/手册/等长placebo；z检验＋95%CI）。实测：来源引用率+67.8pp、边界声明率+36.7pp、严格率+19.0pp（均显著），placebo无效应、零副作用。评分脚本与裁判盲评协议、出入对照表（见手册附录）。A/B/C数据由脚本生成，本地可复现。
联系方式：见 README。

## 三、仓库设置（名称与标签）

- 仓库名：occurrence-development-logic（不改。"发生发展逻辑"是方法论核心名，定位由 About 承载；改名会使已投递链接与 skill 安装命令失效）；
- About（Description）：发生发展逻辑模型（ODL）——面向 Agent 输出纪律与长程一致性的运行时配置纪律层（System Prompt/skill 为加载形态，MIT）；
- Topics（标签）：prompt-engineering runtime-configuration output-discipline agent system-prompt evaluation deepseek llm；
- 设置路径：GitHub 仓库页 → Settings → General（Repository name / Description / Topics）。Issue #1 正文发布时以第二节模板为准（含 v7 数据）。

## 四、关注与合作：诚实预期
1. 有效≠必被关注。有效是必要条件，不是充分条件；
2. 被关注概率 ≈ 数据质量 × 对方评估成本低 × 时机。前两项可控（数据干净、可复现、5分钟看懂），时机不可控；
3. 判断（非事实）：数据有效且可复现时，被注意的概率高于无数据投递；是否回应由对方评估决定，不预判、不承诺；
4. 承诺交付物达到"可直接复现与独立测试"的标准。
