# 分析报告：方法论如何提升 AI 能力（用发生发展逻辑分析其自身效应）

> 本报告用《发生发展逻辑模型 v2.5》自身的四步（发生→发展→应用→自检）分析"方法论如何提升 AI 能力"——既是分析，也是方法论的自我演示（自足性）。

## 〇、先拆题（避免偷换概念）
"AI 能力提升"在本报告中的定义＝**输出行为层**的改善（纪律、一致性、可检验性、单位成本），**不是**权重层的改善（知识、推理深度、创造力）。两者"怎么来的"不同，分析必须分开——这是本报告全部结论的起始窗口。

## 一、发生（怎么来的）：提升的机制根源

1. **输出从哪来**：大模型的输出是条件采样——P(输出｜上下文)。上下文＝系统提示＋历史消息＋参数。任何进入上下文的指令都会**整体移动输出分布**，这是已知的模型工作原理（事实，非假设）。
2. **手册从哪来**：五年方法论（生命起源分析提炼的发生/发展操作）→ 蒸馏为四步规则 → 多轮 A/B 迭代校准（至 v7 三臂判据通过）而来。
3. **起始窗口的推论**：约束层只能强化"分布里已存在的行为"（边界、来源、诚实拒答、拆解——模型训练中本就有这些倾向），不能注入"分布里没有的知识"。所以：**可提升的上限＝行为纪律，提升不了的下限＝知识本身**。

## 二、发展（时空树与位置）：提升的四个分叉

沿"输出生成"这条时间轴，方法论制造了四个分叉，每个分叉在输出行为树上占据一个位置：

### 分叉1 位置＝意图层：把"倾向"变成"纪律"（显式化）
- 机制：模型本会做这些动作（识别意图、写边界），但是概率性倾向。手册把它们写成 system prompt 中的固定指令 → 每次生成的分布被前置约束 → 从"有时做"变"每次都做"。
- 证据：v7 来源引用率 7%→75%（+67.8pp）、边界声明率 2%→38%（+36.7pp）——不是模型变强，是动作被钉死。

### 分叉2 位置＝结构层：轨道化降低长任务漂移（一致性）
- 机制：长任务的敌人不是"不会"而是"每次采样路径不同"（浮动性）。四步骨架把输出过程切成固定结构（判类→拆题→按型→自检），随机性被限制在内容层，结构层被固定。
- 证据：过程指标（要素覆盖、档位声明）成为可程序判定的稳定量；v7 B 组要素覆盖严格率 51/51（100%），结构显式化、可复检。

### 分叉3 位置＝断言层：诚实机制消除"行为性幻觉"
- 机制：幻觉有两类——知识性幻觉（权重层：不知道装知道）与行为性幻觉（不确定却下确定结论、断言无来源）。手册管第二类：给不出来源就不许下断言 → 错误被转化为"无法确认＋可验证路径"或"可追溯断言"。
- 证据：v7 陷阱题三臂均诚实、零编造（9/9，该组已处天花板，无区分度）；机制本身由"来源/边界/数值纪律"条款直接约束断言行为，属可程序判定的硬约束。
- 推论：不是减少错误总数，而是把错误变成**可查、可拒**——对使用者，可核验的输出严格优于不可核验的输出。

### 分叉4 位置＝篇幅层：篇幅随任务档位自适应
- 机制：三档位＋"目的达成优先于效率，效率优先于完备性"→ 短场景不铺开，长任务不省略。
- 证据：v7 按档位自适应——一档问候 56 字符、情感 207 字符（短场景保持短）；三档多句任务 1400+ 字符（长任务给足）。整体中位长度 B 211 vs A 144（+46%）是纪律的价格，按受众与任务分配、不平均摊派。v8 新证据：修复出入对照表条款后，B 组 G7 二档事实 367 字符 vs A 1170（-69%）且质量持平——篇幅纪律的效果直接可见。

### 空间维与推断
- 四个分叉同一主题（输出纪律），互不串树；
- 仍可达的分支：更大 n、更多模型、多轮场景（锚点制）、统计检验；
- 已关闭的分支：影响知识性幻觉——不在约束层能力范围内，任何测试都不应期望它。

## 三、应用（怎么做）：提升发生的落地条件

1. **加载三选一**：对话框粘贴手册全文／Harness 内 /occurrence-development-logic skill／API 作 system prompt（reasoner 并入首条 user 消息）；
2. **生效判据**（预声明）：过程指标显著上升 且 结果指标不显著下降——v7 已通过；
3. **失效条件**（何时不指望它）：天花板题（原生已满分，无提升空间）、纯创作任务、用户明确要求停用（用户即时指令优先）。

## 四、自检（本报告是否合适）

1. 类别：事实评价（机制解释）＋功能实现（成文交付）✓；
2. 来源：v7/v8 数据引自《数据摘要_v8.md》✓；"条件采样"等机制表述基于模型工作原理（事实），并已用"事实/假设"标注区分；
3. 边界：本报告是机制解释＋既有数据支撑，不构成新实验；"提升"仅指纪律/可靠性维度，不宣称权重层提升；
4. 效率：单文件、分节、档位标注，未展开冗余内容。

## 附录：一句话总结
方法论通过把训练中已存在的输出倾向**显式化为固定纪律**（意图层）、**轨道化生成结构**（结构层）、**钉死诚实的断言规则**（断言层）、**量化篇幅成本**（篇幅层），在不动权重的约束层上提升 AI 输出的可靠性、一致性、可检验性与单位成本——它不把模型变聪明，它把模型变可靠。